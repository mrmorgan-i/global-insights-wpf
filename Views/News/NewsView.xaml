<UserControl x:Class="Global_Insights_Dashboard.Views.News.NewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:Global_Insights_Dashboard.Utils.Converters"
             Background="{DynamicResource MaterialDesignPaper}">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
        
        <!-- Article Card Style -->
        <Style x:Key="ArticleCardStyle" TargetType="materialDesign:Card">
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp4" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <materialDesign:Card Grid.Row="0" Padding="20" Margin="0,0,0,20"
                           materialDesign:ElevationAssist.Elevation="Dp1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Title -->
                <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,16">
                    <materialDesign:PackIcon Kind="Newspaper" Width="28" Height="28" 
                                           Foreground="{DynamicResource PrimaryHueMidBrush}" 
                                           VerticalAlignment="Center" />
                    <TextBlock Text="Latest News" FontSize="24" FontWeight="Bold" 
                             Margin="12,0,0,0" VerticalAlignment="Center" />
                </StackPanel>

                <!-- Search Bar -->
                <TextBox Grid.Row="1" Grid.Column="0"
                       Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                       materialDesign:HintAssist.Hint="Search news articles..."
                       materialDesign:TextFieldAssist.HasClearButton="True"
                       Style="{StaticResource MaterialDesignFilledTextBox}"
                       Margin="0,0,12,16" />

                <!-- Search Button -->
                <Button Grid.Row="1" Grid.Column="1"
                      Command="{Binding SearchNewsCommand}"
                      Style="{StaticResource MaterialDesignRaisedButton}"
                      Content="Search"
                      Margin="0,0,0,16"
                      IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolToVisibilityConverter}}" />

                <!-- Filters -->
                <Grid Grid.Row="2" Grid.ColumnSpan="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Country Filter -->
                    <ComboBox Grid.Column="0"
                            ItemsSource="{Binding AvailableCountries}"
                            SelectedValue="{Binding SelectedCountry}"
                            SelectedValuePath="Key"
                            DisplayMemberPath="Value"
                            materialDesign:HintAssist.Hint="Country"
                            Style="{StaticResource MaterialDesignFilledComboBox}"
                            Margin="0,0,12,0" />

                    <!-- Category Filter -->
                    <ComboBox Grid.Column="1"
                            ItemsSource="{Binding AvailableCategories}"
                            SelectedValue="{Binding SelectedCategory}"
                            SelectedValuePath="Key"
                            DisplayMemberPath="Value"
                            materialDesign:HintAssist.Hint="Category"
                            Style="{StaticResource MaterialDesignFilledComboBox}"
                            Margin="0,0,12,0" />

                    <!-- Refresh Button -->
                    <Button Grid.Column="2"
                          Command="{Binding RefreshNewsCommand}"
                          Style="{StaticResource MaterialDesignIconButton}"
                          ToolTip="Refresh news"
                          Width="40" Height="40">
                        <materialDesign:PackIcon Kind="Refresh" />
                    </Button>
                </Grid>
            </Grid>
        </materialDesign:Card>

        <!-- Loading Indicator -->
        <materialDesign:Card Grid.Row="1" 
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                           Padding="20" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                           IsIndeterminate="True" Width="32" Height="32" />
                <TextBlock Text="Loading news..." Margin="16,0,0,0" 
                         VerticalAlignment="Center" FontSize="16" />
            </StackPanel>
        </materialDesign:Card>

        <!-- Content Area -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <!-- No Articles Message -->
                <materialDesign:Card Visibility="{Binding HasArticles, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                                   Padding="40" HorizontalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="FileDocumentAlert" Width="64" Height="64" 
                                               Foreground="{DynamicResource MaterialDesignBodyLight}" 
                                               HorizontalAlignment="Center" />
                        <TextBlock Text="No news articles available" 
                                 FontSize="18" Margin="0,16,0,8"
                                 HorizontalAlignment="Center"
                                 Foreground="{DynamicResource MaterialDesignBodyLight}" />
                        <TextBlock Text="Try adjusting your search criteria or check back later"
                                 FontSize="14" HorizontalAlignment="Center"
                                 Foreground="{DynamicResource MaterialDesignBodyLight}" />
                    </StackPanel>
                </materialDesign:Card>

                <!-- Articles List -->
                <ItemsControl ItemsSource="{Binding Articles}"
                            Visibility="{Binding HasArticles, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Style="{StaticResource ArticleCardStyle}">
                                <materialDesign:Card.InputBindings>
                                    <MouseBinding MouseAction="LeftClick" 
                                                Command="{Binding DataContext.OpenArticleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}" />
                                </materialDesign:Card.InputBindings>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Article Content -->
                                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                        <!-- Title -->
                                        <TextBlock Text="{Binding Title}" 
                                                 FontSize="16" FontWeight="SemiBold"
                                                 TextWrapping="Wrap" Margin="0,0,0,8"
                                                 Foreground="{DynamicResource PrimaryHueMidBrush}" />

                                        <!-- Description -->
                                        <TextBlock Text="{Binding Description}" 
                                                 FontSize="14" TextWrapping="Wrap"
                                                 Margin="0,0,0,12"
                                                 Foreground="{DynamicResource MaterialDesignBody}"
                                                 MaxHeight="60" />

                                        <!-- Metadata -->
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                            <materialDesign:PackIcon Kind="Domain" Width="16" Height="16" 
                                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                                   VerticalAlignment="Center" />
                                            <TextBlock Text="{Binding Source.Name}" 
                                                     FontSize="12" Margin="6,0,16,0"
                                                     Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                     VerticalAlignment="Center" />

                                            <materialDesign:PackIcon Kind="ClockOutline" Width="16" Height="16" 
                                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                                   VerticalAlignment="Center" />
                                            <TextBlock Text="{Binding PublishedAt, StringFormat={}{0:MMM dd, yyyy}}" 
                                                     FontSize="12" Margin="6,0,0,0"
                                                     Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                     VerticalAlignment="Center" />
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Article Image -->
                                    <Border Grid.Column="1" Width="120" Height="80" 
                                          CornerRadius="4" Background="{DynamicResource MaterialDesignDivider}"
                                          Visibility="{Binding UrlToImage, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <Image Source="{Binding UrlToImage}" 
                                             Stretch="UniformToFill" />
                                    </Border>
                                </Grid>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <!-- Last Updated Info -->
                <TextBlock Text="{Binding LastUpdated, StringFormat='Last updated: {0:MMM dd, yyyy HH:mm}'}"
                         FontSize="12" Margin="0,16,0,0"
                         HorizontalAlignment="Center"
                         Foreground="{DynamicResource MaterialDesignBodyLight}"
                         Visibility="{Binding HasArticles, Converter={StaticResource BoolToVisibilityConverter}}" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
